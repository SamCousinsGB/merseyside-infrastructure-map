__pycache__/
*.pyc
.DS_Store

# Build-time only (the published map needs none of this)
node_modules/
# Raw LV cable download is huge (~300 MB) and transient; only its tiles are committed.
# (lv_transformers.geojson IS committed - the map loads it at runtime for the
#  clickable transformer markers.)
lv_cables.geojson
*.geojson.tmp
